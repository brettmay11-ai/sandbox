(() => {
  const main=document.querySelector('main'),header=document.querySelector('header');if(!main||!header)return;
  const pages=[['students','Students','users'],['progress','Progress & Rankings','chart-no-axes-combined'],['featured','Featured Game','star'],['coach','Research Coach','graduation-cap'],['writing','Writing Reviews','notebook-pen'],['teks','TEKS Alignment','book-check']];
  const requested=new URLSearchParams(location.search).get('page'),current=pages.some(([id])=>id===requested)?requested:'students';
  const nav=document.createElement('nav');nav.id='teacher-page-nav';nav.className='max-w-7xl mx-auto px-5 pt-6';nav.innerHTML=`<div class="flex gap-1 overflow-x-auto border-b border-white/10">${pages.map(([id,label,icon])=>`<a href="/teacher?page=${id}" data-teacher-link="${id}" class="relative shrink-0 flex items-center gap-2 px-4 py-3 text-xs font-semibold border-b-2 ${id===current?'border-blue-400 text-white bg-white/[.03]':'border-transparent text-white/45 hover:text-white'}"><iconify-icon icon="lucide:${icon}"></iconify-icon>${label}${id==='writing'?'<span data-writing-count class="hidden min-w-5 h-5 px-1 rounded-full bg-red-500 text-white text-[10px] font-black items-center justify-center"></span>':''}${id==='coach'?'<span data-safety-count class="hidden min-w-5 h-5 px-1 rounded-full bg-amber-500 text-black text-[10px] font-black items-center justify-center"></span>':''}</a>`).join('')}</div>`;header.after(nav);
  const headingPage=text=>text.includes('Featured Game of the Week')?'featured':text.includes('Team Research Coach')?'coach':text.includes('Student Progress and Rankings')?'progress':text.includes('Writing Review Queue')?'writing':null;
  function applyPages(){[...main.children].forEach((child,index)=>{if(index===0){child.style.display='';return}let page=child.dataset.teacherPage;if(!page){page=headingPage(child.textContent)||((child.querySelector('#create-form')||child.querySelector('#student-list'))?'students':null);if(page)child.dataset.teacherPage=page}child.style.display=page===current?'':'none'});const active=pages.find(([id])=>id===current);document.title=`${active?.[1]||'Teacher Dashboard'} | NFL Project`;const intro=main.firstElementChild;if(intro){const title=intro.querySelector('h1'),copy=intro.querySelector('p:last-child');if(title)title.textContent=active?.[1]||'Teacher Dashboard';if(copy)copy.textContent={students:'Create student accounts and assign their season-long NFL teams.',progress:'Review student XP, participation, and classroom rankings.',featured:'Choose and publish the class Featured Game of the Week.',coach:'Set the weekly research focus and review common student questions.',writing:'Read student writing and return helpful feedback.',teks:'Connect the Math, Writing, and Social Studies activities to Grade 4 Texas standards.'}[current]}}
  const observer=new MutationObserver(applyPages);observer.observe(main,{childList:true});applyPages();
  const headerActions=header.lastElementChild,bell=document.createElement('button'),shield=document.createElement('button');bell.type='button';bell.title='Writing submissions waiting for feedback';bell.className='relative w-9 h-9 rounded-lg bg-white/5 hover:bg-white/10 grid place-items-center';bell.innerHTML='<iconify-icon icon="lucide:bell"></iconify-icon><span id="teacher-bell-count" class="hidden absolute -top-1 -right-1 min-w-5 h-5 px-1 rounded-full bg-red-500 text-white text-[10px] font-black items-center justify-center"></span>';bell.onclick=()=>location.href='/teacher?page=writing';shield.type='button';shield.title='Coach safety flags waiting for review';shield.className='relative w-9 h-9 rounded-lg bg-white/5 hover:bg-white/10 grid place-items-center';shield.innerHTML='<iconify-icon icon="lucide:shield-alert"></iconify-icon><span id="teacher-safety-count" class="hidden absolute -top-1 -right-1 min-w-5 h-5 px-1 rounded-full bg-amber-500 text-black text-[10px] font-black items-center justify-center"></span>';shield.onclick=()=>location.href='/teacher?page=coach';headerActions?.prepend(bell);headerActions?.prepend(shield);
  window.refreshTeacherNotifications=async()=>{try{const [writingResponse,safetyResponse]=await Promise.all([fetch('/api/teacher/writing'),fetch('/api/coach/safety-flags')]),writingData=await writingResponse.json(),safetyData=await safetyResponse.json();const count=writingResponse.ok?(writingData.submissions||[]).filter(item=>item.status==='submitted').length:0,safety=safetyResponse.ok?Number(safetyData.unreadCount||0):0,badge=document.getElementById('teacher-bell-count'),safetyBadge=document.getElementById('teacher-safety-count');document.querySelectorAll('[data-writing-count]').forEach(item=>{item.textContent=count;item.classList.toggle('hidden',!count);item.classList.toggle('flex',Boolean(count))});document.querySelectorAll('[data-safety-count]').forEach(item=>{item.textContent=safety;item.classList.toggle('hidden',!safety);item.classList.toggle('flex',Boolean(safety))});badge.textContent=count;badge.classList.toggle('hidden',!count);badge.classList.toggle('flex',Boolean(count));safetyBadge.textContent=safety;safetyBadge.classList.toggle('hidden',!safety);safetyBadge.classList.toggle('flex',Boolean(safety));bell.setAttribute('aria-label',count?`${count} writing submissions waiting for feedback`:'No writing submissions waiting for feedback');shield.setAttribute('aria-label',safety?`${safety} coach safety flags waiting for review`:'No coach safety flags waiting for review')}catch(error){console.warn('Could not load teacher notifications.',error)}};
  window.refreshTeacherNotifications();
})();
