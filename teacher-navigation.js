(() => {
  const main=document.querySelector('main'),header=document.querySelector('header');if(!main||!header)return;
  const pages=[['students','Students','users'],['progress','Progress & Rankings','chart-no-axes-combined'],['featured','Featured Game','star'],['coach','Research Coach','graduation-cap'],['writing','Writing Reviews','notebook-pen']];
  const requested=new URLSearchParams(location.search).get('page'),current=pages.some(([id])=>id===requested)?requested:'students';
  const nav=document.createElement('nav');nav.id='teacher-page-nav';nav.className='max-w-7xl mx-auto px-5 pt-6';nav.innerHTML=`<div class="flex gap-1 overflow-x-auto border-b border-white/10">${pages.map(([id,label,icon])=>`<a href="/teacher?page=${id}" data-teacher-link="${id}" class="relative shrink-0 flex items-center gap-2 px-4 py-3 text-xs font-semibold border-b-2 ${id===current?'border-blue-400 text-white bg-white/[.03]':'border-transparent text-white/45 hover:text-white'}"><iconify-icon icon="lucide:${icon}"></iconify-icon>${label}${id==='writing'?'<span data-writing-count class="hidden min-w-5 h-5 px-1 rounded-full bg-red-500 text-white text-[10px] font-black items-center justify-center"></span>':''}</a>`).join('')}</div>`;header.after(nav);
  const headingPage=text=>text.includes('Featured Game of the Week')?'featured':text.includes('Team Research Coach')?'coach':text.includes('Student Progress and Rankings')?'progress':text.includes('Writing Review Queue')?'writing':null;
  function applyPages(){[...main.children].forEach((child,index)=>{if(index===0){child.style.display='';return}let page=child.dataset.teacherPage;if(!page){page=headingPage(child.textContent)||((child.querySelector('#create-form')||child.querySelector('#student-list'))?'students':null);if(page)child.dataset.teacherPage=page}child.style.display=page===current?'':'none'});const active=pages.find(([id])=>id===current);document.title=`${active?.[1]||'Teacher Dashboard'} | NFL Project`;const intro=main.firstElementChild;if(intro){const title=intro.querySelector('h1'),copy=intro.querySelector('p:last-child');if(title)title.textContent=active?.[1]||'Teacher Dashboard';if(copy)copy.textContent={students:'Create student accounts and assign their season-long NFL teams.',progress:'Review student XP, participation, and classroom rankings.',featured:'Choose and publish the class Featured Game of the Week.',coach:'Set the weekly research focus and review common student questions.',writing:'Read student writing and return helpful feedback.'}[current]}}
  const observer=new MutationObserver(applyPages);observer.observe(main,{childList:true});applyPages();
  const headerActions=header.lastElementChild,bell=document.createElement('button');bell.type='button';bell.title='Writing submissions waiting for feedback';bell.className='relative w-9 h-9 rounded-lg bg-white/5 hover:bg-white/10 grid place-items-center';bell.innerHTML='<iconify-icon icon="lucide:bell"></iconify-icon><span id="teacher-bell-count" class="hidden absolute -top-1 -right-1 min-w-5 h-5 px-1 rounded-full bg-red-500 text-white text-[10px] font-black items-center justify-center"></span>';bell.onclick=()=>location.href='/teacher?page=writing';headerActions?.prepend(bell);
  window.refreshTeacherNotifications=async()=>{try{const response=await fetch('/api/teacher/writing'),data=await response.json();if(!response.ok)return;const count=(data.submissions||[]).filter(item=>item.status==='submitted').length,badge=document.getElementById('teacher-bell-count');document.querySelectorAll('[data-writing-count]').forEach(item=>{item.textContent=count;item.classList.toggle('hidden',!count);item.classList.toggle('flex',Boolean(count))});badge.textContent=count;badge.classList.toggle('hidden',!count);badge.classList.toggle('flex',Boolean(count));bell.setAttribute('aria-label',count?`${count} writing submissions waiting for feedback`:'No writing submissions waiting for feedback')}catch(error){console.warn('Could not load teacher notifications.',error)}};
  window.refreshTeacherNotifications();
})();
