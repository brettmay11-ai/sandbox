/* Featured Game of the Week controls for the teacher dashboard. */
(() => {
  const SPORTS_KEY='ec29dd369c2544a980efca06d3e5b4ad',teams=window.NFL_TEAM_BRANDS||[];let schedule=[];
  const teamName=abbr=>teams.find(team=>team.abbr===abbr)?.name||abbr;
  async function request(url,options={}){const response=await fetch(url,{...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Request failed.');return data}
  const main=document.querySelector('main');if(!main)return;
  const panel=document.createElement('section');panel.className='panel rounded-xl p-5 mb-5';panel.innerHTML=`<div class="flex flex-col md:flex-row md:items-end justify-between gap-3 mb-5"><div><h2 class="font-semibold flex items-center gap-2"><iconify-icon icon="lucide:star" class="text-yellow-400"></iconify-icon>Featured Game of the Week</h2><p class="text-xs text-white/35 mt-1">Choose a matchup from the live 2026 schedule. The home stadium or city photo loads automatically.</p></div><span id="fg-status" class="text-xs text-white/40"></span></div><form id="fg-form" class="grid md:grid-cols-[110px_1fr_140px_auto] gap-3 items-end"><label class="text-xs text-white/50">Week<select id="fg-week" class="mt-2 w-full border border-white/10 rounded-lg px-3 py-2.5 outline-none">${Array.from({length:18},(_,i)=>`<option value="${i+1}">${i+1}</option>`).join('')}</select></label><label class="text-xs text-white/50">Scheduled matchup<select id="fg-matchup" class="mt-2 w-full border border-white/10 rounded-lg px-3 py-2.5 outline-none"></select></label><label class="text-xs text-white/50">Kickoff<input id="fg-time" value="1:00 PM ET" class="mt-2 w-full border border-white/10 rounded-lg px-3 py-2.5 outline-none"></label><button class="bg-blue-700 hover:bg-blue-600 rounded-lg px-5 py-2.5 text-sm font-semibold">Publish Game</button><label class="md:col-span-4 text-xs text-white/50">Class discussion prompt<textarea id="fg-discussion" rows="2" class="mt-2 w-full bg-white/5 border border-white/10 rounded-lg px-3 py-2.5 outline-none" placeholder="Add a math, science, or social studies question about this matchup."></textarea></label></form>`;
  main.insertBefore(panel,main.children[1]||null);
  function displayTime(game){if(!game.Date)return '1:00 PM ET';return new Date(`${game.Date}Z`).toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit',timeZone:'America/New_York'})+' ET'}
  function renderMatchups(selectedAway,selectedHome){const week=Number(document.getElementById('fg-week').value),games=schedule.filter(game=>Number(game.Week)===week);document.getElementById('fg-matchup').innerHTML=games.map(game=>`<option value="${game.AwayTeam}|${game.HomeTeam}" ${game.AwayTeam===selectedAway&&game.HomeTeam===selectedHome?'selected':''}>${teamName(game.AwayTeam)} at ${teamName(game.HomeTeam)}</option>`).join('')||'<option value="">Schedule unavailable</option>';const selected=games.find(game=>`${game.AwayTeam}|${game.HomeTeam}`===document.getElementById('fg-matchup').value);if(selected)document.getElementById('fg-time').value=displayTime(selected)}
  async function load(){try{const [saved,games]=await Promise.all([request('/api/featured-game'),request(`https://api.sportsdata.io/v3/nfl/scores/json/Schedules/2026?key=${SPORTS_KEY}`)]);schedule=Array.isArray(games)?games.filter(game=>game.AwayTeam&&game.HomeTeam):[];const featured=saved.featuredGame||{week:1,away:'NE',home:'SEA'};document.getElementById('fg-week').value=featured.week||1;renderMatchups(featured.away,featured.home);document.getElementById('fg-time').value=featured.time||document.getElementById('fg-time').value;document.getElementById('fg-discussion').value=featured.discussion||'';document.getElementById('fg-status').textContent=`Currently: ${featured.away} at ${featured.home}`}catch(error){document.getElementById('fg-status').textContent=error.message}}
  document.getElementById('fg-week').addEventListener('change',()=>renderMatchups());document.getElementById('fg-matchup').addEventListener('change',()=>renderMatchups(...document.getElementById('fg-matchup').value.split('|')));
  document.getElementById('fg-form').addEventListener('submit',async event=>{event.preventDefault();const button=event.submitter;button.disabled=true;try{const [away,home]=document.getElementById('fg-matchup').value.split('|');if(!away||!home)throw new Error('Choose a scheduled matchup.');const body={week:Number(document.getElementById('fg-week').value),away,home,day:'Sunday',time:document.getElementById('fg-time').value,discussion:document.getElementById('fg-discussion').value};await request('/api/featured-game',{method:'PATCH',body:JSON.stringify(body)});document.getElementById('fg-status').textContent=`Published: ${body.away} at ${body.home}`}catch(error){alert(error.message)}finally{button.disabled=false}});load();
})();
