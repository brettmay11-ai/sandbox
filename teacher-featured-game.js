/* Featured Game of the Week controls for the teacher dashboard. */
(() => {
  const SPORTS_KEY='ec29dd369c2544a980efca06d3e5b4ad';
  const teams=window.NFL_TEAM_BRANDS||[];
  const escapeHtml=value=>String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  async function request(url,options={}){const response=await fetch(url,{...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Request failed.');return data}
  const main=document.querySelector('main');if(!main)return;
  const panel=document.createElement('section');panel.className='panel rounded-xl p-5 mb-5';panel.innerHTML=`<div class="flex flex-col md:flex-row md:items-end justify-between gap-3 mb-5"><div><h2 class="font-semibold flex items-center gap-2"><iconify-icon icon="lucide:star" class="text-yellow-400"></iconify-icon>Featured Game of the Week</h2><p class="text-xs text-white/35 mt-1">Choose the matchup students see on the home page. The home stadium or city photo loads automatically.</p></div><span id="fg-status" class="text-xs text-white/40"></span></div><form id="fg-form" class="grid md:grid-cols-[110px_1fr_1fr_140px_auto] gap-3 items-end"><label class="text-xs text-white/50">Week<select id="fg-week" class="mt-2 w-full border border-white/10 rounded-lg px-3 py-2.5 outline-none">${Array.from({length:18},(_,i)=>`<option value="${i+1}">${i+1}</option>`).join('')}</select></label><label class="text-xs text-white/50">Away team<select id="fg-away" class="mt-2 w-full border border-white/10 rounded-lg px-3 py-2.5 outline-none"></select></label><label class="text-xs text-white/50">Home team<select id="fg-home" class="mt-2 w-full border border-white/10 rounded-lg px-3 py-2.5 outline-none"></select></label><label class="text-xs text-white/50">Kickoff<input id="fg-time" value="1:00 PM ET" class="mt-2 w-full border border-white/10 rounded-lg px-3 py-2.5 outline-none"></label><button class="bg-blue-700 hover:bg-blue-600 rounded-lg px-5 py-2.5 text-sm font-semibold">Publish Game</button><label class="md:col-span-5 text-xs text-white/50">Class discussion prompt<textarea id="fg-discussion" rows="2" class="mt-2 w-full bg-white/5 border border-white/10 rounded-lg px-3 py-2.5 outline-none" placeholder="Add a math, science, or social studies question about this matchup."></textarea></label></form>`;
  main.insertBefore(panel,main.children[1]||null);
  const options=teams.map(team=>`<option value="${team.abbr}">${escapeHtml(team.name)}</option>`).join('');document.getElementById('fg-away').innerHTML=options;document.getElementById('fg-home').innerHTML=options;
  async function load(){try{const {featuredGame}=await request('/api/featured-game');if(featuredGame){document.getElementById('fg-week').value=featuredGame.week||1;document.getElementById('fg-away').value=featuredGame.away||'KC';document.getElementById('fg-home').value=featuredGame.home||'BUF';document.getElementById('fg-time').value=featuredGame.time||'1:00 PM ET';document.getElementById('fg-discussion').value=featuredGame.discussion||'';document.getElementById('fg-status').textContent=`Currently: ${featuredGame.away} at ${featuredGame.home}`}}catch(error){document.getElementById('fg-status').textContent=error.message}}
  document.getElementById('fg-form').addEventListener('submit',async event=>{event.preventDefault();const button=event.submitter;button.disabled=true;try{const body={week:Number(document.getElementById('fg-week').value),away:document.getElementById('fg-away').value,home:document.getElementById('fg-home').value,day:'Sunday',time:document.getElementById('fg-time').value,discussion:document.getElementById('fg-discussion').value};if(body.away===body.home)throw new Error('Choose two different teams.');await request('/api/featured-game',{method:'PATCH',body:JSON.stringify(body)});document.getElementById('fg-status').textContent=`Published: ${body.away} at ${body.home}`}catch(error){alert(error.message)}finally{button.disabled=false}});load();
})();
