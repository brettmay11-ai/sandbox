/* Teacher controls and anonymous question trends for the Team Research Coach. */
(() => {const main=document.querySelector('main');if(!main)return;async function api(url,options={}){const response=await fetch(url,{headers:{'Content-Type':'application/json'},...options});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Request failed.');return data}const panel=document.createElement('section');panel.className='panel rounded-xl p-5 mb-5';panel.innerHTML=`<div class="grid lg:grid-cols-2 gap-6"><div><h2 class="font-semibold flex items-center gap-2"><iconify-icon icon="lucide:graduation-cap" class="text-blue-400"></iconify-icon>Team Research Coach</h2><p class="text-xs text-white/35 mt-1 mb-4">Give the coach a weekly classroom focus.</p><form id="coach-focus-form" class="flex gap-2"><input id="coach-focus" maxlength="500" class="flex-1 border border-white/10 rounded-lg px-3 py-2.5 outline-none" placeholder="Example: Compare stadium capacity and city population"><button class="px-4 py-2 bg-blue-700 rounded-lg text-xs font-bold">Save Focus</button></form></div><div><h3 class="text-xs uppercase font-bold text-white/45 mb-3">Common Questions, Last 30 Days</h3><div id="coach-question-trends" class="space-y-2 text-xs text-white/55">Loading question trends...</div></div></div>`;main.insertBefore(panel,main.children[2]||null);Promise.all([api('/api/coach/context'),api('/api/coach/questions')]).then(([context,data])=>{document.getElementById('coach-focus').value=context.focus?.prompt||'';document.getElementById('coach-question-trends').innerHTML=data.questions.length?data.questions.map(item=>`<div class="flex justify-between gap-3 border-b border-white/5 pb-2"><span>${String(item.question).replace(/[<>]/g,'')}</span><span class="text-white/30">${item.count} asked</span></div>`).join(''):'<span class="text-white/30">No student questions yet.</span>'}).catch(error=>document.getElementById('coach-question-trends').textContent=error.message);document.getElementById('coach-focus-form').addEventListener('submit',async event=>{event.preventDefault();await api('/api/coach/focus',{method:'PATCH',body:JSON.stringify({prompt:document.getElementById('coach-focus').value})});alert('Coach focus saved.')})})();
